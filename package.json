{
    "private": true,
    "workspaces": [
        "packages/visitor",
        "packages/visitor-react"
    ],
    "scripts": {
        "build": "turbo run build",
        "deploy": "yarn build && changeset && changeset version && changeset publish"
    },
    "dependencies": {
        "@changesets/cli": "^2.26.0"
    }
}
