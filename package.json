{
    "private": true,
    "workspaces": [
        "packages/visitor",
        "packages/visitor-react"
    ],
    "scripts": {
        "build": "turbo run build",
        "deploy": "yarn build && changeset version && changeset publish --no-git-tag"
    },
    "dependencies": {
        "@changesets/cli": "^2.26.0"
    }
}
